import ts from "typescript";
import { Metadata } from "../../metadata/Metadata";
export declare const iterate_metadata_atomic: (checker: ts.TypeChecker) => (meta: Metadata, type: ts.Type) => boolean;
